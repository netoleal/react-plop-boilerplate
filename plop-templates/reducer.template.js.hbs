
export const INIT_STATE = {
    loading: false,
    error: null,
};

export default function {{camelCase name}}Reducer( state = INIT_STATE, action = { } )
{
    switch( action.type )
    {
        default:
            return { ...state };
    }
}